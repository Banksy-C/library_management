<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.banksy.lm_server.mapper.AdminMapper">

    <insert id="save">
        insert into admin(name, username, age, sex, phone, address)
        values(#{name}, #{username}, #{age}, #{sex}, #{phone}, #{address})
    </insert>

    <delete id="deleteById">
        delete from admin where id = #{id}
    </delete>

    <select id="listUsers" resultType="com.banksy.lm_server.entity.Admin">
        select id, name, username, age, sex, phone, address
        from admin
        order by id desc
    </select>

    <select id="getById" resultType="com.banksy.lm_server.entity.Admin">
        select id, name, username, age, sex, phone, address
        from admin
        where id = #{id}
    </select>

    <select id="listByCondition" resultType="com.banksy.lm_server.entity.Admin">
        select id, name, username, age, sex, phone, address
        from admin
        <where>
            <if test="name != null and name != ''">
                name like concat('%', #{ name }, '%')
            </if>
            <if test="phone != null and phone != ''">
                and phone  like concat('%', #{ phone }, '%')
            </if>
        </where>
        order by id desc
    </select>

</mapper>